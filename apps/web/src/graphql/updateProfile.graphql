mutation UpdateProfile($input: UpdateProfileInput!) {
  updateProfile(input: $input) {
    id
    currency
    wageMode
    hourlyWage
    monthlySalary
    hoursPerWeek
    effectiveHourlyRate
    isReadyToTrack
  }
}
